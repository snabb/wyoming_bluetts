---
# Home Assistant App Build Configuration (Supervisor add-on manifest)
# https://developers.home-assistant.io/docs/add-ons/configuration#add-on-build-options

# Multi-stage build: builder uses uv:python3.12-bookworm-slim
# Final stage uses python:3.12-slim-bookworm for minimal size
build_from:
    amd64: python:3.12-slim-bookworm
    aarch64: python:3.12-slim-bookworm
